{
  "name": "htbl",
  "version": "1.0.0",
  "description": "Transpile a JSON / CSS style format to HTML",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}